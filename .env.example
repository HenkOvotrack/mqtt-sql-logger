# MQTT SQL Logger Environment Variables
# Copy this file to .env and fill in your actual values
# The .env file should NOT be committed to git (it's in .gitignore)

# =============================================================================
# MQTT BROKER CONFIGURATION
# =============================================================================
MQTT_BROKER_HOST=your-mqtt-broker.example.com
MQTT_BROKER_PORT=1883
MQTT_CLIENT_ID=mqtt-sql-logger-docker
MQTT_USERNAME=your-mqtt-username
MQTT_PASSWORD=your-mqtt-password
MQTT_TOPICS=tele/#,stat/#,shellies/#
MQTT_QOS=1

# =============================================================================
# SQL SERVER CONFIGURATION
# =============================================================================
# Examples for different SQL Server setups:

# Local SQL Server (Windows/Mac Docker Desktop)
# SQL_CONNECTION_STRING=Server=host.docker.internal;Database=MqttLogs;User Id=sa;Password=YourStrong@Passw0rd123;TrustServerCertificate=True;

# Remote SQL Server
# SQL_CONNECTION_STRING=Server=your-sql-server.com;Database=MqttLogs;User Id=your-user;Password=your-password;TrustServerCertificate=True;

# Azure SQL Database
# SQL_CONNECTION_STRING=Server=tcp:your-server.database.windows.net,1433;Initial Catalog=MqttLogs;Persist Security Info=False;User ID=your-user;Password=your-password;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

SQL_CONNECTION_STRING=Server=your-sql-server.example.com;Database=MqttLogs;User Id=your-username;Password=your-password;TrustServerCertificate=True;
SQL_CREATE_TABLE=true

# =============================================================================
# STARTUP CONFIGURATION
# =============================================================================
# Delay in milliseconds before initial MQTT connection attempt
# Useful when deploying with docker-compose where services need time to initialize
STARTUP_DELAY_MS=0

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Valid levels: Trace, Debug, Information, Warning, Error, Critical, None
LOG_LEVEL=Information

# =============================================================================
# EXAMPLE VALUES FOR TESTING
# =============================================================================
# Uncomment and modify these for testing with the working example:
# MQTT_BROKER_HOST=mosquitto
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_TOPICS=test/#,demo/#,sensors/#
# SQL_CONNECTION_STRING=Server=sqlserver;Database=MqttLogs;User Id=sa;Password=YourStrong@Passw0rd123;TrustServerCertificate=True;